

function setup() {
    createCanvas(800, 600);
    
}

function draw() {
    background(255, 255, 255);
    
    circle(400,300, 1);
    fill(0,0,0);
}
if (circle < x, y, 200) {
    scale(x, y, 1)
} else {
    circle(400, 300, 1)
}
